version: '3.4'

services:
  web-api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=https://+:443;http://+:80
      - ConnectionStrings__DefaultConnection=Server=sql_server2022,1433;Database=JobsDb;User=sa;Password=S3cur3P@ssW0rd!;Integrated Security=false;TrustServerCertificate=true
    ports:
      - "7001:80"
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/root/.aspnet/https:ro
    networks:
      - netwrk


  front:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "3000:3000"
    networks:
      - netwrk
      
  identity-provider:
    networks:
      - netwrk
      
  scheduler:
    environment:
      - EventBusConnection=rabbitmq
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=admin
      - ConnectionStrings__HangfireConnectionStrings=Server=sql_server2022,1433;Database=Hangfire;User=sa;Password=S3cur3P@ssW0rd!;Integrated Security=false;TrustServerCertificate=true
    networks:
      - netwrk
      
  notifier:
    environment:
      - EventBusConnection=rabbitmq
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=admin
    networks:
      - netwrk
      
  rabbitmq:
    networks:
      - netwrk      

  db:
    networks:
      - netwrk
  
networks:
  netwrk:
    external: false
    name: netwrk


